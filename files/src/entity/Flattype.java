package entity;

/**
 * This enum represents the type of flat
 */
public enum Flattype {
    /**
     * The type of flat is tworoom flat
     */
    TWOROOM,
    /**
     * The type of flat is threeroom flat
     */
    THREEROOM
}